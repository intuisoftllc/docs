---
title: "Content Moderation & Semantic Prefilter"
description: "The semantic prefilter and template-moderation operation guarding custom templates."
---

A semantic prefilter plus the template_moderation operation guard user-authored templates against abuse.

## What this covers

- _shared/ai/moderation/ semantic prefilter (prefilter-math.ts, poison-concepts.ts, semantic-prefilter.ts) + the template_moderation op; moderation_failed / moderation_* parsing errors.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
