---
title: "RLS & Service-Role Model"
description: "Why every table has RLS enabled with no policies, and how Edge Functions access data."
---

Every table enables Row Level Security with no policies — the safe default — while Edge Functions use the service role key.

## What this covers

- Every table has ENABLE ROW LEVEL SECURITY with no policies = the safe default; Edge Functions use the service role key (bypass RLS), the anon key gets nothing.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
