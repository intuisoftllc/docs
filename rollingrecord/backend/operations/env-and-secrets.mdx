---
title: "Environment Variables & Secrets"
description: "The per-function secrets, auto-injected values, and the documentation rule."
---

Each function reads a least-privilege set of secrets; some are auto-injected, and every variable must be documented.

## What this covers

- Dedicated per-function secrets (REVENUECAT_WEBHOOK_SECRET, STORAGE_JANITOR_SECRET, EXTRACT_RUNNER_SECRET, ANALYTICS_INGEST_SECRET, ADMIN_OPS_SECRET/ADMIN_OPS_HMAC_KEY, ANTHROPIC_API_KEY, ASSEMBLYAI_API_KEY, EMBEDDING_API_KEY); auto-injected SUPABASE_URL/SUPABASE_SERVICE_ROLE_KEY; the README env-var documentation HARD RULE; never read .env.local.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
