---
title: "user-sync-preferences"
description: "The endpoint that reads and updates a user's per-group sync toggles."
---

user-sync-preferences exposes GET/PATCH over the six per-group sync toggles that gate what replicates.

## What this covers

- GET/PATCH the user_sync_preferences row (6 sync toggles); _shared/sync/groups.ts + preferences.ts; per-group sync gating.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
