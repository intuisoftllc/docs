---
title: "Sync Shared Modules"
description: "The shared sync helpers that translate between wire shape and row shape."
---

Shared modules under _shared/sync/ define the constants, cursor, entities, and wire-to-row transforms every sync endpoint reuses.

## What this covers

- _shared/sync/ — constants.ts, cursor.ts, entities.ts, transform.ts (wire to row shape), groups.ts; the column-spine parity between DB shape and wire shape.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
