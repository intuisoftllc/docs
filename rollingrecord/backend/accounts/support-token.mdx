---
title: "generate-support-token"
description: "The user-initiated support code and its abuse defenses."
---

generate-support-token mints a short-lived, single-use 6-digit code a user reads to a support agent for verification.

## What this covers

- A user-initiated 6-digit code for support verification (60-min, single-use, return-existing semantics); the support_token_generate bucket with abuseEventOnReject; brute-force defense on the verify side.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
