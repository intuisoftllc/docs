---
title: "analytics"
description: "The server-to-server ingest endpoint for the Python eval harness."
---

analytics is a content-free, server-to-server ingest endpoint used by the Python eval harness to record run metadata.

## What this covers

- Server-to-server ingest (ANALYTICS_INGEST_SECRET) for the Python eval harness; v1 op log_eval_run upserts l_eval_run; content-free (no customer data) per the privacy HARD RULE.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
