---
title: "Continuations & Merge"
description: "Merging a continuation recording onto the original timeline."
---

A continuation recording is merged back onto the original timeline by a background worker, with a standalone-file fallback.

## What this covers

- `MergeWorker` merging a continuation onto the original timeline; `V2PlaybackMergeUi`.
- Standalone-file fallback behavior and its effect on mark positions.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
