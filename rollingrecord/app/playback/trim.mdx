---
title: "Trim & Splice"
description: "Trimming recordings and the splice/merge utilities behind it."
---

Trimming lets users cut recordings, backed by splice and merge utilities.

## What this covers

- `V2TrimScreen` / `V2TrimViewModel` / `V2TrimSheets`.
- `AudioSplicer` and `AudioMerger` utilities backing trim and segment edits.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
