---
title: "Waveform Visualization"
description: "How waveforms are extracted, cached, and rendered."
---

Waveforms are extracted and cached in the background, then rendered live during recording and playback.

## What this covers

- `WaveformEngine`, `WaveformExtractor`, `WaveformCache`, and the background `WaveformExtractionWorker`.
- `V2MiniWaveform` and `RecordingWaveBackground` rendering; the "Always show waveform" setting.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
