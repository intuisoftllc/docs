---
title: "Koin Dependency Injection"
description: "App-level Koin modules and the cross-user safety enforcement."
---

Dependency injection is handled by Koin, split into app-level singletons and per-user scoped instances.

## What this covers

- App-level modules: `AuthModule`, `DatabaseModule`, `DaoModule`, `DataSourceModule`, `NetworkModule`, `RepositoryModule`, `ServiceModule`, `ViewModelModule`, `WorkerModule`, `AnalyticsModule`, `V2Module`.
- The app-level `single` vs user-scoped distinction, and the cross-user-safety enforcement (`UserScopedCacheRegistryTest`).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
