---
title: "Rolling Record Mode"
description: "Indefinite capture within a storage cap by pruning the oldest segments."
---

Rolling mode records indefinitely within a user-defined storage cap, pruning the oldest segments to make room like a dashcam.

## What this covers

- Indefinite capture within a user storage cap by pruning oldest segments; `StorageManager` + `StorageLimitHandler`.
- Rolling storage-usage indicator and live session viewer (Pro).
- Protecting an in-progress segment from pruning.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
