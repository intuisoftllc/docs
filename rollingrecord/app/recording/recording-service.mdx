---
title: "Recording Foreground Service"
description: "The foreground service lifecycle, state machine, and notification."
---

Recording runs in a foreground service driven by a controller and a state machine, surfaced to the UI through observable state.

## What this covers

- `RecordingService` lifecycle, `RecordingController` orchestration, and the `RecordingStateHandler` state machine.
- `RecordingNotificationManager` foreground notification and controls.
- `RecordingServiceState` exposure to `V2RecordingScreen` / `V2RecordingViewModel`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
