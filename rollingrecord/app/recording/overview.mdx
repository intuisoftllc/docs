---
title: "Recording System Overview"
description: "How manual and rolling capture share one foreground pipeline."
---

Both capture modes run through a shared foreground recording pipeline built around a small set of service objects and data models.

## What this covers

- Manual vs Rolling modes (`RecordingMode`) and how the foreground pipeline is shared between them.
- Core service objects: `RecordingService`, `RecordingController`, `RecordingServiceState`.
- Data models: `RecordingSession`, `Recording`, `RecordingProfile`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
