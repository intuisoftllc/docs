---
title: "Extraction Templates (Blueprints)"
description: "Built-in and custom extraction templates."
---

Extraction is driven by blueprints — built-in server-owned templates plus user-created custom ones.

## What this covers

- `BuiltInBlueprints` + `BlueprintRepository` / `BlueprintEntity` / `BlueprintDao`; `ContextPermissions`.
- Custom template creation/editing (`V2BlueprintEditorScreen`, `UserTemplateModels`) with AI-assisted drafting.
- Built-in template IDs are append-only (server-owned; see the `CLAUDE.md` HARD RULE).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
