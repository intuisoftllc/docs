---
title: "Context Extraction Pipeline"
description: "Turning recordings into sections, insights, and action items."
---

Extraction distills a recording into structured context — sections, insights, and action items — metered in true audio minutes.

## What this covers

- `ContextExtractionWorker` plus `ExtractionPlanner`, `ExtractionResultsApplier`, `ExtractionResultsMapper`.
- Extraction metered in true audio minutes with internal chunking; over-cap recordings stay pending and auto-resume.
- Output types: sections, insights, action items (`ContextCardRepository`, `ActionItemRepository`).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
