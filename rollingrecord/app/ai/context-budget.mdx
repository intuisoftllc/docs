---
title: "Context Size Meter & Budget"
description: "The on-device context-size meter and its budget."
---

Each context shows a size meter computed on-device against the extraction input-token budget — informational, not a hard gate.

## What this covers

- `ContextBudget` (agentcore) computed on-device from sections + tasks vs `extraction_max_input_tokens`.
- The context options sheet meter (`ContextOptionsSheet`); informational only, no gate.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
