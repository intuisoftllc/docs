---
title: "Tasks & Action Items"
description: "Action items extracted from recordings and managed as tasks."
---

Extraction produces action items that surface as tasks the user can create, complete, and sync.

## What this covers

- `ActionItemRepository` / `ActionItemEntity`; `V2TasksScreen` / `V2TasksViewModel` / `V2TaskFormSheet`; `TaskDeadlines`.
- Completing/creating tasks and their sync transform (`ActionItemTransform`).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
