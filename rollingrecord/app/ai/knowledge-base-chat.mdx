---
title: "Knowledge Base Chat"
description: "Conversational chat over recordings and contexts."
---

KB chat is a conversational assistant over the user's recordings and contexts, with saved threads and scope filtering.

## What this covers

- `KnowledgeBaseAdvisor` driving the conversational chat over recordings/contexts; `V2KnowledgeBaseScreen`, `V2ThreadsScreen`.
- Saved threads (`SearchThreadRepository`, `SearchThread`/`SearchMessage` entities), context-scope filter (`V2ChatFilterSheet`), swipe-to-reply.
- Chat UI components: `V2ChatBubbles`, `V2ChatInputBar`, `V2ChatSettingsSheet`, `V2MarkdownText`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
