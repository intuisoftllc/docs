---
title: "Semantic & Keyword Search"
description: "Server-side embeddings with an offline keyword/FTS fallback."
---

Search combines server-side semantic embeddings with an offline keyword/FTS fallback and on-device ranking.

## What this covers

- `RecordingSemanticSearchEngine`, `KnowledgeBaseSearchEngine`, `ContextSearchRanker`, `SemanticMarginFilter`.
- Server-side embeddings (RR-487) with keyword/FTS offline fallback (`KeywordIndexDao`/`Entity`); `SearchApi` / `SearchModels`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
