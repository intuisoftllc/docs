---
title: "Embeddings Index & Reconciliation"
description: "Storing embeddings locally and keeping the index current."
---

Embeddings are stored locally and continuously reconciled so search stays current with the user's content.

## What this covers

- `EmbeddingDao`/`EmbeddingEntity` storage; `EmbeddingReconcileWorker` + `EmbeddingReconcileScheduler` keeping the index current.
- `EmbeddingBundleCache` / `EmbeddingBundleFormat` bundle handling.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
