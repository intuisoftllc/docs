---
title: "Firebase App Check"
description: "Attaching App Check tokens to backend requests."
---

Backend requests carry a Firebase App Check token, warmed at startup.

## What this covers

- `AppCheckTokenInterceptor` attaching `X-Firebase-AppCheck`; `AppCheckWarmup` and `AppCheckBootstrap` (debug provider).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
