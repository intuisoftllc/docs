---
title: "Networking & Edge API Layer"
description: "The API clients and error handling for the backend."
---

The app talks to the backend through typed API clients with a shared error model that mirrors the server registry.

## What this covers

- `SupabaseApi`, `AiEngineApi` / `AiEngineClient`; `ApiResult` / `EdgeErrorBody` / `EdgeErrorReporter` / `EdgeTag`.
- Error-registry mirror: `AiEngineErrorCode` / `AiEngineErrorMapper` (kept in sync with the server registry).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
