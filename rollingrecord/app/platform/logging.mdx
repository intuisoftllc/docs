---
title: "Diagnostic Logging"
description: "The shareable on-device diagnostic log and its safety policy."
---

A rolling on-device log captures field diagnostics that users can share for support, under a strict safety policy.

## What this covers

- `AppLogger` + `FileLogger` rolling on-device log (the `save = true` policy from `CLAUDE.md`) shareable for support.
- What is and isn't safe to persist.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
