---
title: "Broadcast Receivers"
description: "The broadcast receivers that drive scheduling and playback."
---

Broadcast receivers handle boot re-arming, scheduled-recording alarms, and playback actions.

## What this covers

- `BootReceiver` (re-arm schedules), `ScheduleAlarmReceiver`, and `PlaybackActionReceiver` + `PlaybackCommandBus`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
