---
title: "Connectivity, Battery & Session Monitors"
description: "System monitors that gate background work."
---

System monitors gate background work on connectivity, battery, and session state.

## What this covers

- `ConnectivityMonitor` / `NetworkConnectivity`, and `BatteryGate` / `BatteryMonitor` gating background work.
- `SessionMonitor` and `NetworkErrorClassifier`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
