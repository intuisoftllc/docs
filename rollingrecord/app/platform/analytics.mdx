---
title: "Analytics & Crash Reporting"
description: "Content-free analytics and crash/kill reporting."
---

Analytics and crash reporting capture operational signals only — never customer content.

## What this covers

- `AnalyticsTracker` / `AnalyticsClient` / `AnalyticsEvent`, `SupabaseFirebaseAnalyticsClient`, `AnalyticsDao` buffering.
- `CrashReporter`, `OsKillSentinel`, `FlushTrigger`; content-free metrics only (per the privacy HARD RULE).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
