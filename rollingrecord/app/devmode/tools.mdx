---
title: "Developer Tools & Diagnostics"
description: "The debug-only diagnostic screens and flag discipline."
---

Dev Mode bundles diagnostic screens for sync, logs, transcription, and chat traces.

## What this covers

- `SyncDebugScreen`, `LogDiagnosticsScreen` / `LogFullViewScreen` / `AppLogUploader`, `TranscriptionDiagnosticsScreen`, `DevChatTracesScreen`.
- `DevBackupManager` and `DevOnboardingPreviewScreen`; `DevFlags` injection-site discipline.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
