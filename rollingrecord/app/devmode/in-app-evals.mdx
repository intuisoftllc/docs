---
title: "In-App Eval Harness (debug)"
description: "The on-device eval harness in the debug build."
---

The debug build includes an on-device eval harness that exercises the real agent loop.

## What this covers

- The `evalmode` package: `Runner`, `Judge`/`Verdict`, `SystemUnderTest`, `CorpusStrategy`, `FixtureSeeder`, `CostLedger`.
- `EmbeddingEvalRunner` and how it exercises the real agentcore loop for local evaluation.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
