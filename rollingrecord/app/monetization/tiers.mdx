---
title: "User Tiers & Feature Map"
description: "The four tiers and how features are gated by them."
---

Features are gated by four stacked tiers, each a strict superset of the one below.

## What this covers

- `UserTier` enum (FREE, PRO, PRO_CLOUD, PRO_CLOUD_AI) and `atLeast()` gating; the authoritative feature-map comment.
- `ProAccess` resolution and tier-driven UI (`V2TierBadge`).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
