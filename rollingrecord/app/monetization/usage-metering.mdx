---
title: "Usage Metering & Caps"
description: "Monthly usage counters, caps, and the usage screen."
---

Metered features are tracked against monthly caps on calendar-month periods.

## What this covers

- `UsageRepository` (owner-stamped, the `UserScopedCache` reference impl), `UsageApi`/`Client`, `UserUsageSnapshot`.
- Calendar-month periods (YYYY-MM, UTC) per `CLAUDE.md`; `V2UsageScreen` meters and `UsageResetFormat`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
