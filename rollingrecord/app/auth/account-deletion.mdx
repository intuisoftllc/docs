---
title: "Account Deletion"
description: "The account-deletion flow and its server cascade."
---

Account deletion drives the server delete cascade and tears down local data, aligned with GDPR erasure.

## What this covers

- `AccountDeletionCoordinator` and `DeleteUserRequest` driving the server `delete-user` cascade.
- Local teardown and GDPR erasure alignment.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
