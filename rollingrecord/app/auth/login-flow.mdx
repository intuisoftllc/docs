---
title: "Login & Sign-Up Flow"
description: "The email-OTP and Google sign-in flow."
---

Sign-in is a step flow supporting email OTP and Google, on a login-scoped navigation graph.

## What this covers

- `V2LoginScreen` and step screens: `V2EmailEntryScreen`, `V2OtpScreen` (magic-link 6-digit OTP), `V2NameEntryScreen`.
- Google sign-in via `GoogleCredentialProvider`; login-scoped nav (`V2LoginNavHost`).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
