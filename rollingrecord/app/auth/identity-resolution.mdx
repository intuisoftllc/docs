---
title: "Identity Resolution"
description: "Mapping the Clerk identity to the backend user id."
---

The Clerk identity is resolved to the backend's canonical user id and wired into session switching.

## What this covers

- `IdentityRepository` and `ResolveIdentityDto` mapping the Clerk identity to the backend `auth_user_id`.
- Multi-account switching hooks into `UserSessionManager`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
