---
title: "Known Accounts / Account Switching"
description: "Remembering signed-in accounts and switching between them."
---

The device remembers signed-in accounts for quick switching, with strict session isolation.

## What this covers

- `KnownAccount` / `KnownAccountsRepository` remembering signed-in accounts on the device.
- Session isolation guarantees on switch.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
