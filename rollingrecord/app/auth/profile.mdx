---
title: "Account Profile"
description: "Display name, profile photo, and profile caching."
---

The account screen manages the user's display name and profile photo, cached for fast rendering.

## What this covers

- `V2AccountScreen` / `V2AccountViewModel`; display name (`DisplayNameSanitizer`) and `V2EditNameDialog`.
- Profile photo (`ProfilePhotoController`, `ProfilePhotoFiles`, `V2ProfileAvatar`) stored in Clerk; `ProfileCache` / `CachedUserProfile`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
