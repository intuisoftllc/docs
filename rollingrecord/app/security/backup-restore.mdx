---
title: "Encrypted Backup & Restore"
description: "Exporting and restoring an encrypted local backup."
---

Users can export and restore an encrypted backup container of their local data.

## What this covers

- `BackupExporter` / `BackupRestorer`, `BackupModels`, `BackupSqlCipherFile`.
- Container/manifest format: `RrBackupContainer`, `BackupManifest` (crypto module).

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
