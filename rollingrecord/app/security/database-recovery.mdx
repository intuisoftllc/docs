---
title: "Database Recovery States"
description: "How the app handles a database that can't be opened."
---

When the database can't be opened, the app surfaces recovery states rather than silently failing.

## What this covers

- `DatabaseRecoveryState` and the `DatabaseModule` key-mismatch wipe/recovery path.
- Recovery UI: `V2DatabaseRecoveryDialog`, `V2FatalDbErrorScreen`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
