---
title: "Recovery Codes"
description: "One-time recovery codes and their role in DEK recovery."
---

A recovery code issued at onboarding backs the account's key-recovery path.

## What this covers

- `RecoveryCodeApi` / `RecoveryCodeApiClient` and models (`RecoveryCodeRequest`/`Response`, `AcknowledgeResponse`).
- `V2RecoveryCodeSaveScreen` during onboarding and its role in DEK recovery.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
