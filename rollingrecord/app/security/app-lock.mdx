---
title: "App Lock"
description: "The lock overlay that gates the whole app."
---

App Lock gates the entire app behind an overlay driven by a lifecycle-aware controller.

## What this covers

- `AppLockController`, `AppLockRepository`, `AppLockArmedCache`; `V2LockOverlay` / `V2LockOverlayViewModel` and `AppLockUi` hosting.
- The arm/disarm lifecycle and how the overlay gates the whole app.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
