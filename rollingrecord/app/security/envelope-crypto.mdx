---
title: "Envelope Encryption Primitives"
description: "The pure-JVM crypto primitives shared with smoketest."
---

Content encryption uses envelope primitives from the shared crypto module, mirroring the backend column convention.

## What this covers

- crypto module: `EnvelopeCrypto` (AES-256-GCM), `TextCrypto`, `KeyDerivation`, `EncryptedField`.
- The `_ct`/`_iv` column convention mirrored on the client; parity with smoketest.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
