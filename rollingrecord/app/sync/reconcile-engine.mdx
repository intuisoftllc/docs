---
title: "Pull & Reconciliation"
description: "Applying server changes and resolving conflicts."
---

The pull side reconciles server changes into the local database, resolving conflicts by outcome.

## What this covers

- `ReconcileEngine` merge outcomes (accepted / server_wins / rejected); `PendingPullApplyDao`/`Entity` deferral.
- `SyncEpisodeProgress` tracking and `SyncRejectionDebugLogger` diagnostics.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
