---
title: "Wire Format & Transforms"
description: "The wire shape and per-entity transforms used by sync."
---

Sync converts between local rows and the wire shape through per-entity transforms.

## What this covers

- `WireEntity` / `WireTransform` and the per-entity transforms under `data/sync/transform` (`RecordingTransform`, `ContextTransform`, `TranscriptTransform`, `SettingsTransform`, etc.).
- `SyncApi` endpoints and `SyncTransformHelpers`.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
