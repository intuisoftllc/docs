---
title: "Sync Preferences"
description: "Per-group sync toggles and device-settings sync."
---

Users control what syncs via per-group toggles, and device settings sync across devices too.

## What this covers

- `SyncPreferencesRepository`, `SyncPreferencesApi`/`Client`, `SyncPreferencesResponse`; `SettingsSyncObserver`.
- `SettingsBlob` device-settings sync and cross-device consistency.

<Note>
Scaffold generated from the codebase map — expand with full detail, examples, and specifics.
</Note>
